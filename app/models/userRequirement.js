/*var mongoose = require('mongoose');
var bcrypt   = require('bcrypt-nodejs');

// define the schema for our user model
var userRequirementSchema = mongoose.Schema({
     userRequirement
               : {
        openingHour  : String,
        closingHour    : String,
        Days:String,
        MaximumPerson:String
               }
});
module.exports = mongoose.model('User', userRequirementSchema);*/